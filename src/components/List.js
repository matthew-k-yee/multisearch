import React from 'react' 

const List = props => {

  return (
    <h1>hi</h1>
  )
}

export default List

   // {props.data.map((country, i)=> {
    //   return (
    //     <td key={i}>{country.country}</td>
    //   )
    // })} 